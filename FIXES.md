# Groundtruth — bug-fix report (v0.9.0 → v1.1.1)

Groundtruth audits whether an agent did what it was asked. **v0.8.0** turned that lens on **Groundtruth itself** — first empirically (reading 14 of its own live sessions), then through two adversarial review passes. **v0.9.0** adds a new honesty class (**6 — dropped symbol / dangling reference**) and two new enforcement rungs (pre-commit + CI), built the same way across three more review passes (see the Class-6 section below). Every finding was reproduced against the real code, fixed at the root, and locked with a regression test. Self-check went from 242 → **362 checks**; the red-team suite grew to **14/14** (a Class-6 dangling-ref rail added).

**Method** (the same one Groundtruth preaches): for each fix we asked *"what made the wrong thing cheaper, and what's the cheapest way for a smart agent to make this check go green without doing the work?"* — then removed that path and left a test so it can't come back.

---

## How they were found

1. **Empirical scorecard.** We read every finding Groundtruth emitted across 14 of its own recent sessions (~22 findings). The majority were false positives, clustering into four buckets. That data — not intuition — set the fix priority: *precision of existing detectors first, new features last.*
2. **Adversarial code review (two passes).** An independent reviewer re-ran every repro against live code and actively tried to break the changes — crafting inputs to slip a real secret, stub, or `eval()` past the gate, and to make an incomplete task go green. Pass 1 found 2 criticals + 6 lesser; pass 2 confirmed those closed and found 1 residual. All fixed.

---

## Security

### S1 · A live secret was demoted from BLOCK to warn by attacker-adjacent text  *(critical)*
- **Symptom:** `const awsKey = "AKIA…real…"; // see example` passed the block gate. So did `EXAMPLE_TOKEN = "ghp_…"` and `sk_live_… // put YOUR key here`.
- **Root cause:** the example-key demotion tested the **whole line** (`SYNTHETIC_MARKER_RE.test(line)`) — but a marker anywhere on the line ("example", a `FAKE_` var name) is attacker-choosable. The design comment literally said "decide on content, not location," then decided on location.
- **Fix:** demote **only** when the matched key token itself is allowlisted or self-marks (`AKIAIOSFODNN7EXAMPLE`). A real-format key blocks regardless of surrounding text.
- **Test:** a real key with a `FAKE_` var name and with `// example` both still block; a token that self-marks (`…EXAMPLE`) demotes.

### S2 · Block-severity false positive on published example keys
- **Symptom:** the canonical AWS docs key (`AKIAIOSFODNN7EXAMPLE`), which lives in the project's own red-team fixture, produced a **block**.
- **Fix:** a small allowlist of published example credentials + a synthetic-marker check **on the key token** (`EXAMPLE`/`SAMPLE`/`FAKE`/…) demotes those to warn; everything else still blocks. Letter-boundaried so a marker glued into a key's charset can't spoof it.
- **Test:** example key → warn; real high-entropy key → block. (The red-team's own probe key was moved off the allowlisted value so its block-path scenarios still bite.)

---

## Stub / marker precision (self-match class)

### P1 · The stub detector flagged its own regex literal
- **Symptom:** `const STUB_MARKER_RE = /\b(TODO|FIXME|XXX|HACK)\b/` was reported as a stub. So were `// TODO` examples quoted inside doc comments and JSON strings.
- **Root cause:** markers matched raw bytes with no notion of *position* — "firing was cheaper than lexing."
- **Fix:** one shared position-aware layer. Markers (`TODO`/`FIXME`/…) count **only in comment/prose position**, with inline-code (`` `…` ``) treated as quotation; phrase-idioms (`NotImplementedError`, `throw…not implemented`, Rust `todo!()`) count **only in code position**; strings, regex literals, JSON, and fenced code are quotation everywhere. Block-comment and markdown-fence state thread across lines.
- **Also:** a marker immediately followed by enumeration punctuation (`TODO/FIXME`, `HACK)`) is a *list documenting* markers, not a real one.
- **Result:** self-audit Class-2 findings dropped and the engine's own source is now **0** self-matches (remaining hits are genuine test-fixture idioms).

---

## Compiled-rule precision

### R1 · `never use eval()` fired on `page.$eval()`
- **Symptom:** a rule forbidding the global `eval()` matched member access — Playwright's `page.$eval(...)` and `x.eval()`. (A prefix like `myeval()` was never matched — no word boundary — so only the `.`/`$` member-access cases were affected.)
- **Root cause:** `\b` treats `.` and `$` as word boundaries, so `\beval\s*\(` matched `.eval(` / `$eval(`.
- **Fix:** at the shared rule normalizer, a leading `\b` before an identifier in a **call rule** (pattern contains an escaped `\(`) upgrades to `(?<![\w$.])` — global `eval()` still fires, member calls don't. Identifier/column rules (no `\(`, e.g. `\bsignup_date\b`) are untouched so they still match `row.signup_date`. Applied at runtime, so it fixes already-armed rules with no re-arm.

### R2 · A rule fired on the very doc that declares it
- **Symptom:** a rule compiled from `ARCHITECTURE.md` ("never use eval") flagged `ARCHITECTURE.md` itself.
- **Fix:** a compiled rule never fires on its own declaring file (provenance is recorded on the rule; zero hand-maintenance).

### R3 · A call rule fired on the forbidden call *mentioned in a comment*
- **Symptom:** the `eval` rule flagged a comment that merely says `// … the global eval() …`.
- **Fix:** a call rule tests against the code portion only (comments stripped); comment-targeting rules (`@ts-ignore`) are left whole.

---

## Scan scope

### Z1 · Findings on the tool's own state and on out-of-repo throwaways
- **Symptom:** secrets/stubs/rules fired on `.claude/groundtruth/*` (Groundtruth's own state) and on scratchpad/`/tmp`/absolute-path files reaching the scan via the tool-ledger.
- **Fix:** the content scanners skip GT's own state (already covered by the integrity signature — a stronger sensor), absolute paths, `../` escapes, and `tmp/scratchpad/` throwaways. Audit the delivery, not the sandbox.

### Z2 · Phantom-import false positives on fixture strings
- **Symptom:** `./helper`, `./example` inside a test file's `const diff = "+import x from './helper'"` were reported as unresolved imports.
- **Fix:** an import counts only when its keyword survives string-blanking (an import-shaped substring *inside a string literal* is test data, not an import). Self-audit Class-4 findings: **3 → 0**.

---

## Tamper / integrity noise

### T1 · The out-of-band tamper caveat fired as noise on every MCP/Bash session
- **Symptom:** "referee state changed OUT-OF-BAND — compiled-rules.json" appeared as a warn/block finding on ordinary sessions. Grounding showed every session ran **unsigned** (`keyed:false`), so the signal couldn't be authoritative anyway.
- **Fix:** split by regime. **Trustworthy** (key set + valid signature) → a loud `tamper` finding. **Unsigned/best-effort** → a quiet `integrity_note` (info-tier) that renders as a card footer, **is not injected into the next turn, and does not flip the verdict**. Real forgery (an invalid signature) stays loud. Enforcement lives in the signature + CI, exactly as the tool already states.

---

## Open-loop ledger (the completeness backstop)

The ledger tracks each ask that names a deliverable and only closes it when the deliverable grounds in the diff. This release reworked it into a tiered model — and review found the rework had opened new holes, all now closed.

### L1 · A conversational aside became a phantom open loop  *(the original motivation)*
- **Symptom:** *"I can see a 304 in `handleUpload`, it's fine, no fix needed"* minted a task that could never close (no diff), nagged every turn, and escalated to block on any "done".
- **Fix:** a deterministic **request/non-request gate** — an observation/question with no surviving action verb is demoted to a **SOFT** aside (tracked, shown once, never blocks, auto-expires), not dropped. "…is fine, no fix needed" is an aside; "…but fix the 500 in `retry.js`" keeps its verb and stays a real task. *No LLM* — a model in front of every stop would void the "deterministic, offline" guarantee and could silently suppress a real open loop.

### L2 · A comment mention closed a code task  *(cheap-green on the close side)*
- **Symptom:** the cheapest way to "deliver" symbol `fooBar` was `// TODO: fooBar` — a comment mention grounded the task.
- **Fix:** a symbol deliverable grounds only when it lands in **non-comment code**; filenames still ground via diff headers.

### L3 · A generic "all done" escalated every unrelated pending task to block
- **Symptom:** one "Done!" flipped every open task to a blocking finding (the block-spam seen repeatedly in real sessions).
- **Fix:** **per-token done-match** — a task escalates only if the completion claim references *that task's own* deliverable token.

### L4 · The agent could demote its own task by naming the deliverable in its reply  *(critical, review)*
- **Symptom:** ask "add a csv export to `report.js`"; reply "Done — I added the csv export to `report.js`" → the ask was demoted to SOFT and went green.
- **Root cause:** a "token-novelty" heuristic treated any token appearing in *agent* text as a reference — but the agent's own reply is agent-authored, so it demoted the very ask it answered. An audited agent must never be able to shape its own verdict.
- **Fix:** token-novelty via agent text was **removed entirely**. Paste-provenance stays — it's grounded in the *user's* text (a `` ``` `` fence or a `file.js:line` stack-trace ref), which the agent can't author.

### L5 · A soft/reference token closed a hard task  *(review)*
- **Symptom:** "`handleUpload` is fine, but fix `retry.js`" — a diff touching `handleUpload` (the soft token) closed the hard `retry.js` task.
- **Fix:** a task grounds only on the tokens of its **own tier**; a soft token can't green a hard task.

### L6 · nag-once made a still-pending task read as "Told & Done"  *(review)*
- **Symptom:** after surfacing once, a pending hard task returned no finding — so the card printed "🟢 every ask delivered / Told & Done" while the ledger still held it open. Quiet == green is a lie.
- **Fix:** a pending hard task **always** emits a card finding (so the card honestly shows "N pending"); a repeat surface is marked `quiet`, which suppresses only *injection into the agent's context* (nag-once), not the card. A matching done-claim un-quiets and escalates.

### L7 · Paste-provenance was structurally dead
- **Symptom:** a `` ``` ``-fenced code paste in a question (`why does this crash?`) minted a hard open loop for the pasted symbol.
- **Root cause:** clause-splitting shredded the fence before the paste check ran.
- **Fix:** paste-stripping is computed on the **full ask once**, before clause-splitting.

### L8 · Polite/omitted imperatives were demoted to SOFT
- **Symptom:** "could you move `gameState` into `state.js`?" and "can you drop the retry from `queue.js`?" were treated as questions and never tracked.
- **Fix:** added `move`/`drop`/`split`/`port`/`convert`/… to the request-verb set; broadened the observation gate so "the 304 **is fine**" (bare copula) reads as an aside while a real verb survives the strip.

### L9 · An honest "still pending" disclosure was false-blocked; a far-away guard word dodged a real block  *(two-sided, review pass 2)*
- **Symptom (A):** "`upload.js` not yet done" was falsely escalated to block. **(B):** "Done — `upload.js` is complete, nothing pending, will close out" (a genuine false-done) dodged the block down to warn.
- **Root cause:** the done-claim negation guard reused the ask-oriented clause splitter and tested the **whole clause** — so it both shredded "not yet done" and let a far-away "pending/will" (about *other* work) suppress a real escalation.
- **Fix:** decide on **proximity** — a ±3-word window around each occurrence of the token — against a strong-signal negation set (`still`/`pending`/`not`/`will`/`yet`/…). `remaining`/`left` were excluded because they invert ("nothing remaining"); `still` already covers genuine "remaining" cases. This closes both reported cases (the far-away/`remaining` dodge and the shredded "not yet done"). It does **not** eliminate the ceiling — see the residual note below: within the ±3-word window the ambiguity is irreducible in **both** directions.

---

## Class 6 — dropped symbol / dangling reference (the refactor "everything preserved" lie)

The new honesty class + its two new enforcement rungs (pre-commit, CI), built precision-first across three review passes. Symptom → cause → fix → the fixture that pins it.

### C6-1 · A naive "defined-nowhere" check false-fired on every rename / merge / casing-change
- **Symptom:** flagging any removed function/method defined nowhere in the tree meant a plain rename (`computeTax`→`calculateTax`, callers updated), a two-into-one merge, or a camelCase↔snake_case change — all behaviour-preserving — read as "dropped."
- **Root cause:** chasing *intent* (was it renamed?) rather than the observable *consequence*.
- **Fix:** fire only on a **dangling reference** — the removed name is still **called** somewhere. A rename/merge/recasing that updates its callers leaves nothing dangling → silent; one that misses a caller fires on the genuinely broken call and quotes it. No rename-detector, no normalize step, no disclosure list.
- **Test:** `c6 FP: clean merge …`, `c6 FP: rename with all callers updated`, `c6 TP: rename with a MISSED old-name caller`.

### C6-2 · Library/builtin method-name collisions false-fired
- **Symptom:** removing a local `flush`/`get`/`render` fired because `stream.flush()` / `cache.get()` (unrelated objects, methods defined out-of-tree) looked like dangling calls.
- **Root cause:** counting `anyObj.name(` as a call — but its resolution runs through the receiver's TYPE, invisible to grep.
- **Fix:** receiver-gated classification ("Option R") — only a **bare** `foo(` (minus a stdlib-globals set) and a **self** `this.foo()`/`super`/`self` count; every other receiver abstains (the same abstain-over-guess posture as Class 4 on package imports).
- **Test:** `c6 FP: cross-object receiver collision`, `c6 FP: common method name get only ever .get(-called`, `c6 FP: bare stdlib global collision`.

### C6-3 · TypeScript signatures broke def recognition — both directions
- **Symptom:** `foo<T>(x): T {` and `foo = (a): number => {}` weren't recognized as defs, so a typed method MOVE false-fired (Layer-1 missed the re-add) and a typed method DROP was silently missed (never a candidate). TS is a primary refactor target; the first e2e was untyped JS, so it slipped through review round 1.
- **Fix:** the def regexes accept optional generics `<…>` and a `: ReturnType` between `)` and `{`/`=>`; `familyOf` covers `.mts`/`.cts`.
- **Test:** `c6 TS: typed method MOVE … → silent`, `c6 TS: typed method DROP … → fires`, `c6 TS: typed arrow field`, `c6 .mts`.

### C6-4 · The pre-commit installer wrote a silently-inert hook on any spaced path  *(the cardinal sin)*
- **Symptom:** installing from a path with a space (`/Users/John Doe/…`, Windows `C:\Users\First Last\`, a cloud-synced `.claude`) produced a `.git/hooks/pre-commit` whose `[ -f "$GT" ]` guard always failed → the hook installed but never ran, forever — exactly the silent inertness the tool exists to prevent.
- **Root cause:** `new URL(import.meta.url).pathname` percent-encodes spaces (`John%20Doe`) — and the same idiom sat in **two pre-existing** sites (rule compilation, and the `main()` entry-guard, which would make the *whole tool* inert on a spaced path). The lazy fix was the root-cause fix: swap the idiom at all three sites, not just the new one.
- **Fix:** `fileURLToPath(import.meta.url)` everywhere. The generated hook body is a pure, exported `preCommitHookScript()`: single-quoted (POSIX-escaped) path; fail-open guards with stderr breadcrumbs for a missing `node` (GUI git clients run a minimal PATH → an unguarded `exec node` exits 127 and blocks *every* commit) and a missing script (stale path after a plugin update).
- **Test:** `c6 hook script: space-safe single-quoted path (no %20), node-guard …`, `c6 hook script: a single-quote in the path is POSIX-escaped`, plus a spaced-path install e2e.

### C6-5 · CI-mode (`--diff-range`) inertness / false-fire / injection edges
- **Shallow-clone silent pass:** `actions/checkout` defaults to `fetch-depth:1`, so a PR base ref is absent → `git diff` errors → the `git` helper swallows it → empty diff → a silent PASS on a broken PR. **Fix:** verify each range endpoint resolves (and for three-dot ranges that a merge-base exists) and FAIL LOUD otherwise.
- **`git grep <tree>` prefix bypass:** hits come back `HEAD:path` → the `(^|/)dist/` / `excludedScanPath` path-**prefix** filters silently miss (`HEAD:dist/…` has no leading `/`) → a CI false-fire. **Fix:** strip the `<tree>:` prefix in the tree-grep branch.
- **Argument injection:** the range reaches `git` via `execSync`. **Fix:** `parseDiffRange` accepts only a safe ref token and rejects shell metachars and `-`-leading (`--upload-pack=…`) segments. **Test:** `c6 CI parseDiffRange: REJECTS shell injection … AND -leading arg-injection`.

---

## Residual / documented limits (honest scope)

- **Class 6 — lossy merge / gutted body:** a method whose NAME survives (callers happy) but whose LOGIC was silently dropped is behaviour-equivalence — semantic, not deterministic — so Class 6 is silent on it (partial cover: a stub/TODO body trips Class 2). Also silent: a dropped **public** method with no in-repo caller (precision over recall), dynamic dispatch `obj[name]()`, and overloads/same-name-on-another-class (name-level, not `Class.method`-level). C/C++ abstain (prototype ≠ def on one line). Three-dot ranges across **unrelated histories** are rejected loudly, never silently scanned.
- **Bare `TODO` as prose inside a comment** (no backticks) is indistinguishable from a real one deterministically — the one self-match class that remains, and near-zero on a normal repo.
- **Computed-member `eval`** (`window["eval"](x)`, `globalThis.eval`, aliasing) is not caught by the call-position rule — a pre-existing limit, not opened by this change.
- **Deterministic-NL ceiling** on request/aside classification and claim-negation: position and structure cover ~95%; genuine natural-language understanding is off the deterministic table by design. Each such shortcut is marked in-source. **This ceiling is two-sided, not purely fail-safe.** The claim-negation window (L9) shows both failure modes with the token's file *absent* from the diff (i.e. a real false-done):
  - **Unsafe miss (bounded):** a negation/deferral word placed within ~3 words of the deliverable token demotes a genuine false-done from block → **warn** — e.g. `"upload.js done, will do next"`, `"finished upload.js, todo elsewhere"`. It is *bounded*, never silent/green: the task stays **pending**, the warn stays on the card, and the Class-3 no-op independently warns on a past-tense false claim — so the backstop degrades to warn, never to a clean pass.
  - **Fail-safe miss:** an honest negation *outside* the window (`"upload.js is a really important file that is not yet done"`) false-blocks an honest agent — hides nothing.
  Widening the window trades one for the other; there is no fixed-window setting that closes both. The `±3` window is a `NOTE:`-marked tuning knob in-source, biased toward block on the *far-away* case (the security-relevant one), while accepting this bounded near-token miss.

The through-line: **abstain or fail safe where a check is provably correct; where it isn't (the NL ceiling), degrade to a bounded warn — never to a clean green — and never let the audited agent influence its own verdict.**

---

## v0.9.1 — installer-arms-rules UX + the integrity hardening it forced

`/groundtruth-setup` became the **installer** (arms clean rules inline, in warn, on consent) and `/groundtruth-rules` the **ledger** (inspect / `unarm <id>`); the verdict card now prints each compiled rule's `[id]` so silencing a wrong one is a single command; `/groundtruth-block on` grew an itemized, fire-count-backed confirmation. Two adversarial review passes drove the security-relevant fixes below.

- **C6 dropped-symbol was silently INERT on Windows.** *Symptom:* the Class-6 dangling-ref check never fired on Windows (red-team scenario J failed there; the mocked unit tests never exercised the real `git grep`). *Root cause:* `mkGrepTree` built a shell string and single-quoted the pattern, but `execSync` uses `cmd.exe` on Windows, where single quotes aren't delimiters — so `git grep` searched for the literal quoted string and matched nothing. *Fix:* `execFileSync('git', ['grep', …, pat, …])` with an argument array — no shell, no quoting — the same pattern `compile-rules.mjs` already used. *Test:* red-team is now **14/14 on Windows** (was 13/14); scenario J proves the real grep path.

- **Covert re-disarm: a stale ratification excused a later out-of-band rules-disarm.** *Symptom:* in a key-configured session where the user ever ran `/groundtruth-rules` or `/groundtruth-setup`, an agent could `echo '[]' > compiled-rules.json` via Bash many turns later and the referee snapshot excused it (the ratifying command was in the session-cumulative set). Adding `/groundtruth-setup` as a rules-writer (needed for inline arming) widened this. *Root cause:* ratification was session-cumulative — a command invoked once excused *any* later change. *Fix:* the referee snapshot now carries a **signed transcript high-water mark** (`mark` = count of slash-command invocations at write time, signed inside `{files, observed, mark}` — see the `observed` fix below); `compareSnapshot` excuses a changed target only by a ratifier invoked **since** that mark (`freshRatifiers = commandInvocations.slice(mark)`), and each Stop re-snapshots — advancing legit/unchanged targets while **holding** an unratified change at its old hash so it keeps firing. A monotonic name set would have false-flagged a *second* legitimate arming; a count/position does not. *Tests:* `snapshot-scope` (a no-fresh-ratifier disarm fires; a fresh one is excused), `freshRatifiers REGRESSION` (a 2nd same-name arming past the mark is fresh), `advanceSnapshot` (unratified change held), plus the parseTranscript ordered-invocations test.

- **Later-ratifier launder: a routine command laundered a held disarm.** *Symptom:* a covert disarm that fired every turn was silenced the moment the user ran *any* ratifying command later — even a read-only `/groundtruth-rules list` — whose fresh ratifier excused the disarmed state and advanced it into the baseline. *Root cause:* the excuse tested only "changed vs baseline + a fresh ratifier," not *when* the change happened, so a divergence held from a prior turn was indistinguishable from a change made this turn. *Fix (reframed from the command to the file — no argument-parsing):* the snapshot now also carries a **signed per-target `observed`** (the actual current hash each Stop). A fresh ratifier excuses a change only if it happened THIS turn — `cur !== observed` — so a held disarm (`cur === observed`) is never excused or blessed into the baseline by a later routine command, while a legitimate arming (which *writes* the file on the command's own turn, `cur !== observed`) is excused with zero false positives. *Tests:* `observed-close` (a fresh ratifier fires on a held disarm, excuses a this-turn change) + `advance CLOSE` (a held divergence is never blessed) — **389 unit checks total**, red-team 14/14.

**Residual named, not hidden (the honest scope).** One variant is *not* closable in-session, and it is a pre-existing class, not introduced here:
  - **Snapshot rollback-replay:** `refsnapshot.json` cannot snapshot itself and a Bash restore of it is invisible to both rails; an older validly-signed snapshot carries a lower `mark` (and stale `observed`), so restoring it re-widens the fresh window. HMAC proves authenticity, not recency, and any in-session anti-replay state is itself a rollback-able on-disk file — a *minimal* rollback (by one) evades any small this-turn-commands threshold, so no false-positive-free in-session check closes it. Same class as "the D9 snapshot is a rollback-able on-disk file," **unchanged** since D9; bounded to interactive key-regime sessions. It is closed by the **CI/pre-merge gate** (`--diff-range` takes a fresh per-run snapshot — no interval to ride), the documented real enforcement boundary. The `observed`-hash close above forces an attacker off the passive "wait for a routine command" path and onto this conspicuous snapshot manipulation.

The through-line holds: **in-session integrity is tamper-*evidence*, not prevention — the deterministic prevention boundary is a CI/pre-merge run in a trust domain the agent's shell can't reach.**

---

## v0.9.2 — Windows: rule compilation was silently dead + setup surfaces the AI-rules opt-in

- **`compile-rules.mjs` never ran on Windows → the rules / directive-override feature (the differentiator) was silently inert there.** *Symptom:* `proposed-rules.json` was never written on Windows, so `/groundtruth-setup` reported "Rules ✗ — init hasn't run" no matter what, and no rule could ever be armed — the "Told & Ignored" bucket was dead on the platform its own author develops on. *Root cause:* the run-as-main guard `import.meta.url === \`file://${process.argv[1]}\`` — `import.meta.url` is `file:///C:/…` (forward slashes) while `process.argv[1]` is `C:\…` (backslashes), so they never matched and `main()` never ran when the engine shells out to it (`recompileRules`). *Fix:* `import.meta.url === pathToFileURL(process.argv[1]).href` (normalizes both, and resolves a relative argv). Same class as the C6 grep-quoting fix. *Test:* a new CLI regression spawns the script the way the engine does and asserts `main()` fires + writes the file — the pure-function tests missed it entirely because they `import { compile }` directly, bypassing the guard. **393 checks; red-team 14/14.**

- **`/groundtruth-setup` surfaces `/groundtruth-rules-ai` as a text pointer (opt-in, never auto-run).** When a repo has substantial prose rule-docs but the literal extractor found few candidates, setup now shows a one-line tip: the AI pass is the *one* place Groundtruth spawns agents / uses an LLM — and only to **propose** rules, routed through the same grounding + approval gate (nothing arms until you approve); every per-turn audit stays deterministic and offline. Solves discovery without moving the model into the default path or eroding the deterministic-by-default identity — *point, don't run.*

## v0.9.3 — integrity-key posture: setup stops writing it, and the keyless-baseline warn becomes a quiet note

- **The setup command's closing offer wrote `GROUNDTRUTH_KEY` into `settings.local.json` — a shell-readable location its own posture says the key must NOT live in.** *Symptom:* accepting *"set up settings.local.json"* generated a `randomBytes(24)` key and wrote it locally; the audited agent's own shell can then `printenv` it, so it's best-effort **evidence, not prevention** — and a session whose integrity snapshot predated the key emitted a confusing *"integrity unverified — snapshot unsigned"* warn every turn until the next restart. Setup was bundling a CI-shaped secret into the in-session offer, against the settled *"in-session = evidence; enforcement = CI, in a trust domain the agent can't reach"* posture. *Fix:* the closing offer no longer writes `GROUNDTRUTH_KEY` at all — it wires the status badge and (deliberate, opt-in) `GROUNDTRUTH_BLOCK` only; the integrity-key item now points the user to a **CI secret** + the `--diff-range` workflow. If a key is already set locally, setup names the trade-off and the restart-to-re-sign path instead of papering over it. *Why block stays but the key goes:* the block flag is solid in-session (the agent can read it but can't change what the hook sees); a key it can read **and use to re-sign a forgery** is not.

- **A keyless baseline under a now-set key fired a loud "the agent rewrote its OWN state" tamper/warn every turn — for a benign state.** *Symptom:* when a session's snapshot was taken at a keyless SessionStart and a key appeared later (setup, a manual `export`, a CI env), `compareSnapshot` emitted `cls:'tamper', sev:'warn'` — rendered under the alarmist "verdict may be compromised" header and injected into the next turn — even though nothing changed. *Root cause:* the branch conflated "signing not active yet" with "downgraded to evade the seal." But `keyed:false` means there was **never a signed seal**, so nothing could have been downgraded/forged. *Fix (verified by Fable, with the security trace):* that one branch drops to a quiet `cls:'integrity_note', sev:'info'` (the T1 regime-split) — a ⚪ footer, not injected, doesn't flip the verdict, never the tamper header; wording is "signing not active this session — restart to sign; nothing was tampered." A **real** downgrade (`keyed:true` + missing/invalid sig) stays loud (separate branch), and an actual out-of-band change is still surfaced by the per-target hash loop. *Documented ceiling:* this also quiets the delete-signed-then-write-keyless dodge to info — accepted, because a shell-readable key already lets a competent agent write a valid signed forgery (fully quiet today); real enforcement is a key held out of the agent's shell + a CI gate.

- **`/groundtruth-setup` is now truly the single point for rules — and it gates the AI pass on doc-presence, not candidate count.** *Symptom (two bugs):* (1) the command only *mentioned* `/groundtruth-rules-ai` as text and said "never invoke it from setup," so the AI pass stayed a separate manual step — against the 0.9.1–0.9.2 intent that setup arm literal rules, run the AI pass, and approve, all in one flow; (2) it gated even the mention on *"few/no literal candidates found,"* so a doc-heavy repo that yielded 26 literal rules was told nothing — exactly backwards. *Fix:* setup now **offers to run** `/groundtruth-rules-ai` inline (explicit opt-in) and arms its clean proposals the same way as literal ones; the offer is gated **only** on whether rule-docs exist (`CLAUDE.md`/`AGENTS.md`/`**/SCHEMA.md`/`**/ARCHITECTURE.md`/`docs/*.md`/skills/agents) — no docs → skip (nothing to read); any docs → offer, regardless of literal count. The offer states plainly that it fans out an agent **once**, only to *propose* rules, and that **no model is ever called again** after approval (every per-turn audit stays deterministic + offline).

## v0.9.4 — Class-1 honesty precision (the meta-discussion self-matches) + a syntax-check tier + forbid_path grounding

Driven by two real sessions. One was spent *discussing* the honesty check and kept tripping it — Class-1 text-matches claims, so a message quoting its own trigger words self-matches. The other was a live Vercel `ERR_REQUIRE_ESM` post-mortem that exposed a hole: `node --check` counted as "tests ran," so raw-node green passed for the transpiled runtime that actually ships. Fable verified every FP against the live regex, before and after.

- **Class-1 false-green FPs: four verified misfires, one of them a false BLOCK.** *Symptom:* the "claimed tests/build pass" check fired on non-claims — "the build **should be verified**" (🔴 block), "**I'll** make the tests pass", "the tests **don't** pass yet", a far counterfactual "even if **I'd** watched the build go green" — and it self-matched any message *quoting its own pattern* ("tests/build … pass/green"). *Root cause:* the hypothetical guard was (a) tested against the **whole message**, not the claim's sentence — so a hypothetical in one sentence silenced a real claim in another, and a same-sentence-but-far counterfactual slipped its 3-word proximity window; (b) its verb set (`pass|green|succeed`) was **narrower than the claim's** (`…|verified|all clean`), so "should be verified" dodged it; (c) it knew no contractions (`I'll`) or negations (`don't`). *Fix:* scope both the hypothetical **and** a new negation test to the **sentence** holding the claim; mirror the guard's verb set to the claim's; add contraction/negation/counterfactual markers; suppress a **quoted pattern** (a slash/pipe between letters or regex group syntax inside the matched span — a real claim says "tests pass", never "pass/green"). *Test:* 9 new checks — each FP asserted with **no** test command run (so a leak would BLOCK), plus must-still-fire cases (a real "Tests pass." is **not** silenced by a later hypothetical sentence).

- **`node --check` (and `tsc`/`cargo check`/…) counted as a test run — the ESM-incident hole.** *Symptom:* an agent that ran only `node --check`, then claimed "tests pass", sailed past the no-test-ran block — but a syntax/compile check is not the transpiled runtime that ships (the `.mjs`-under-`api/` → `ERR_REQUIRE_ESM` class). *Fix:* when the claim noun is specifically `tests` and **every** recognized command is a syntax/type check (`node --check`, `node -e`, `tsc`, `deno check`, `cargo check`, `go vet`, `py_compile`, `ruby -c`), warn *"only a syntax/type check ran — verify in the runtime that ships"* (warn, not block: the noun is specific so FP is low, but a legit tsc-as-test turn shouldn't halt). *Test:* only-`node --check` → warn; a real `npm test` → clean; a "build passes" claim after `tsc` → **not** demoted (noun is build, not tests).

- **`forbid_path` seed rules always grounded "armable" — even when a matching file was already committed.** *Symptom:* a path rule (e.g. `^api/.*\.mjs$`) with **no** `line_re` was grounded by `codeHits(…, undefined, …)`, which threw and was caught as "0 hits" → the rule looked safe to arm even when it already matched committed files (an over-broad path rule slipped the `/groundtruth-rules` review triage). *Root cause:* path rules were grounded by **grepping content** — but a path rule forbids a file *existing*, so it must be checked against the tracked-file **list**. *Fix:* a new injectable `pathHits(root, file_re, ls=gitLsFiles)` grounds path rules against `git ls-files`; a committed match → `review`, no match → `armable` (fires only on new files). *Test:* a repo with a committed `api/existing.mjs` grounds the `^api/**.mjs` rule `review`; a non-matching `^lib/**.mjs` rule grounds `armable`.

- **A zero failure-COUNT read as a failure — a SUCCESS line false-warned.** *Symptom:* `TEST_FAIL_RE`'s count alternatives (`\b\d+\s+(?:failing|failed|failures?)\b`, `Tests?:\s*\d+\s+…`) matched a leading `0`, so a passing run's summary — "12 passed, **0 failed**", "Tests: **0 failing**" — tripped the Class-1 *"a test run looks like it reported failures — double-check"* warn even though nothing failed. (Surfaced by Fable's SHIP pass as a pre-existing FP, confirmed against HEAD.) *Fix:* the two count alternatives require a **non-zero** leading digit (`[1-9]\d*`); the RSpec (`examples, [1-9]\d* failures`) and JUnit (`Failures: [1-9]`) branches already did. *Verification (Fable):* zero-count success lines across jest/vitest/pytest/go/cargo/RSpec/JUnit/TAP go quiet, and **24 real-failure formats still fire** (incl. multi-digit `27 failed` and the inversion `0 passed, 1 failed`); the only string the stricter count no longer matches is `01 failed`, which no runner emits. *Follow-up (the other direction — a real MISS):* ava's `1 test failed` was silently missed — the word "test" between the count and "failed" defeats `[1-9]\d*\s+failed`, and it can be ava's only failure signal — so a `[1-9]\d*\s+tests?\s+(?:failed|failing)` alternative (still non-zero-gated) now catches it. We deliberately did **not** broaden `FAIL\b`→`FAILED` for pytest's bare `FAILED path::test` line: the word "failed" in "0 failed" would re-open the FP, and Fable confirmed against real pytest 8.4.2 (every flag combo) that a completed run always prints the `N failed` summary we already catch — the bare line only appears in self-mangled output (`pytest | grep`). *Test:* "12 passed, 0 failed" / "0 tests failed" → silent; "2 failed" / "1 test failed" → warn.

**Adversarial verification (Fable, two FIX-FIRST rounds).** Round 1 found **five** holes the first cut missed — a false-BLOCK regression (a claim whose noun+verb straddled a `!`/`?` fell outside the sentence guard), the `forbid_path` grounding hole *re-opening* on a `(?i)` file_re (raw `new RegExp` threw where `compileRuleRe` wouldn't), and three block-rail evasions/misfires (a path in the span like `apps/web` tripping the quoted-pattern suppressor; a *trailing* counterfactual laundering a leading real claim; the demotion misfiring on `npm test && tsc` and on `tsc` inside a filename). All five were fixed — noun+verb must now share a sentence (`[^.!?\n]*`), `pathHits` grounds via `compileRuleRe`, the suppressor requires a slash/pipe joining two claim *keywords*, the counterfactual is tested only on the sentence prefix, and the demotion is evaluated per shell-segment. Round 2 confirmed those closed but caught **the segment-anchoring's own regression** — it made the demotion inert on the *dominant* real forms (`npx tsc`, `./node_modules/.bin/tsc`, `FOO=1 tsc`) while over-narrowing to bare `tsc` — plus two block-tier misses: a hedged EARLIER sentence shadowing a real later claim (first-match-only scan), and a noun/noun slash ("Build/tests pass") wrongly read as a quoted pattern. Fixed: the demotion allows real runner/path/env prefixes (with `tsc(?=\s|$)` still excluding `tsc` in a filename), the claim scan is global (first surviving match wins), and the suppressor requires a *verb* keyword on one side of the slash. A third, narrow pass then confirmed all three closed with **no new regression** (SHIP) — probing 16+ real runners (`npx jest`, `yarn test`, `pnpm exec vitest`, …) to prove the prefix-allowance doesn't mislabel a genuine test run as a syntax check. Every finding across the two fix rounds is pinned with a regression test. A follow-up then fixed the zero-count `TEST_FAIL_RE` FP above, Fable-verified SHIP (24 real-failure formats still fire).

Self-check **400 → 428**; red-team **14/14**.

## v0.9.5 — completeness/honesty precision from Groundtruth's own worst-case session

A long session spent *building and discussing Groundtruth itself* — a conversation about the auditor, spanning a second repo, with a verifier sub-agent running most turns — is the pathological worst-case for a text-matcher, and its own findings were a precision backlog. Three FP classes closed, each Fable-verified; the frozen corpus never covered these (esp. `async_done`, which had zero corpus entries), so this is their precision purge.

- **A file named in a READ request was tracked as a hard, block-escalating open-loop.** *Symptom:* "look at cluePrompts.js" / "review the report" minted a pending deliverable that never closes (it was a read, not a write) and escalated to block on any "done"-ish reply. *Root cause:* the deliverable classifier had a read-target carve-out for *convention docs only*; a code file read had none. *Fix (Fable, two FIX-FIRST rounds):* a `READ_INTENT_RE` gate DEMOTES a clause's tokens to **soft** (surfaced once, auto-expires, never blocks) when no write/action verb co-occurs. The first cut *dropped* them inside `extractTokens` — an invisible false-negative ("read and rewrite parser.js" tracked nothing), violating the module's own "demote, never drop" invariant; reworked to demote inside `classifyDeliverables`, and `REQUEST_VERB_RE` extended with the delivery verbs the drop had masked (`rewrite`, `optimize`, `simplify`, `harden`, `dedupe`, …). *Test:* read-intent → no hard task; a read + a write verb → still hard.

- **Discussing Groundtruth's OWN state files by bare basename false-fired the Class-3 no-op.** *Symptom:* "I updated seed-rules.json" flagged "claimed a change … absent from the diff." *Fix:* `NONREPO_OR_TOOL` now excludes bare `seed-rules.json` / `procedures.json` (was only `compiled|proposed-rules.json`) — the whole `.claude/groundtruth/` path was already excluded; this closes the bare-basename gap. Path-qualified genuine edits still flag.

- **`async_done` cried wolf 23×/session (13/13 sampled FP) — the largest class.** *Symptom:* a bare "done/shipped/clean" token in ordinary prose ("would've **shipped** bugs", "the fix is **clean**") + a verifier sub-agent pending (`bgPending`≈always in this workflow) collapsed the check to a bare-token match on ~every status message. *Root cause:* the loose `COMPLETION_RE` is a word-list, not a completion *claim*. *Fix (Fable consult + a 13→1 transcript replay):* async_done now keys on a turn-verdict **STAMP** (`COMPLETION_STAMP_RE` — a line-anchored "Done!/All done —/🟢 Told & Done", or generic-subject "everything's done / the work is complete"), not a mid-prose token; line 969's symbol-gated use keeps `COMPLETION_RE`. *Rejected:* a suppress-on-acknowledgment predicate — it would delete the check's founding fixture (an acknowledged pending task IS what it catches) and invert incentives (append "…still running" for immunity). The exploit *"Done! …oh, the deploy's still running"* **still fires** — the stamp is the tell. *FN accepted:* a non-stamped same-deliverable lie ("The deploy finished successfully" while it runs) — the Tier-2 case the old check only "caught" by muting itself. *Test:* the FP storm silent; FIXTURE 1 + the exploit + generic-subject still fire.

*Honest boundary (not fixed):* the cross-workspace no-op FPs — a file edited in *another* repo, discussed here, flagged absent from *this* diff — remain the documented cross-workspace limitation, not separable from a real no-op without semantics.

Self-check **428 → 440**; red-team **14/14**. All three Fable-verified.

## v1.0.0 — block visibility, test-gaming detectors, and the precision purge that came with them

The 1.0 release. Two capability additions (in-session block UX + test-gaming detection, plus sealing the auditor's own code), built the same precision-first way — five Fable review rounds, every confirmed false positive reproduced against the real engine and locked with a regression test. It also closed the deepest FP the tool ever had: the one it fired **on its own reasoning about itself**. Self-check **440 → 495**; red-team **14/14**.

**Features**

- **Block visibility (Defect B) — a block could halt or escalate INVISIBLY in the VS Code extension.** *Symptom:* block mode fed the corrective payload to the model and the outcome only to `systemMessage`, which VS Code does not render (issues #15344, #50542, the latter closed *won't-fix*); the escalate branch was `systemMessage`-only too and the loop re-entrant — so a block (including a false one) could loop silently with the user staring at a spinner. *Fix:* the block/escalate outcome is stamped into `findings.json` and re-surfaced at the next prompt as a loud `🔴 BLOCKED/ESCALATED — ACTION NEEDED` banner via `additionalContext` (the one channel that reliably renders) — an unresolved block is never silently lost. Plus a best-effort live pop: open the verdict in the running editor (`code -g`, or macOS `open -b <__CFBundleIdentifier>` when the CLI isn't installed) + a desktop toast; detached, fail-open, deduped per catch, suppressed for throwaway repos under `tmpdir`. *Honest boundary:* live surfacing is best-effort (headless/remote has no desktop); the guarantee is the next-prompt banner + CI.

- **Test-gaming detectors (AG-B / AG-C) — new warn-only honesty classes.** `test_exclusion`: a success claim while the tests were SKIPPED/EXCLUDED (surefire `<skipTests>`/`<exclude>`, gradle disable, jest `testPathIgnorePatterns` naming a test path, coverage-threshold *lowering*, or `-DskipTests`/`gradle -x test` flags). `test_weakened`: an existing test WEAKENED (strict assertion → loose by **net count** — migration-safe, since strict→strict doesn't drop the count — or a net-new `@Disabled`/`it.skip`). Both run on the **git-only diff** (the tool-ledger replays a file's unchanged context lines as `+`, which would false-flag a pre-existing skip — Fable), are claim-gated by a negation/quote-aware `claimsSuccess()`, and exempt brand-new test files (no removed lines = legit TDD).

- **Rule Zero for the auditor's own code (AG-A).** `hooks/*.mjs` are sealed into the integrity hash snapshot (absolute-path targets resolved from `import.meta.url`). It inherits the existing regime split — **info-only in-session** (no dogfooding FP when a contributor edits the hook) and a real **block in the keyed/CI rung**, where a pristine runner hashes the deployed hook vs the release. Named honestly: a *targeted* in-session self-edit runs the already-neutered file that turn, so this is evidence, not prevention — the guarantee lives in CI.

**Precision (every FP below was confirmed on a real verdict card, then Fable-verified)**

- **Class-1 fired on QUOTING "tests pass", not claiming it.** *Symptom:* a session *about* a test-tamper tool echoes finding text / docs saturated with "tests pass"; the substring matcher read the quote as a first-person claim and — in block mode — silent-looped on it. *Fix:* `stripQuotedForClaim` blanks fenced/blockquote and excuses inline-`code` spans that fully contain a match (a straddling `` All `tests` pass `` still fires); a reported-speech guard excludes attribution (`claimed/flagged/said … tests pass`). And Class-1 `!ran` was **demoted block → warn**: a prose-grounded conjunct may not hard-block in-session (the tiering rule: artifact-grounded classes block, prose-grounded ones warn / block only in CI).

- **Class-1 `failed`-branch fired on an HONEST partial-pass disclosure.** *Symptom:* "15/16 pass; team-mode.test.js is a pre-existing failure I disclosed" tripped `TEST_FAIL_RE`, because the honest output prints `FAIL …team-mode.test.js` next to `pass` — the sensor can't attribute a `FAIL` line to a suite. *Fix:* suppress the `failed` branch (only that branch) when the claim OWNS the failure — a pass-fraction N<M **bound to a test word** (so a date `12/2024` isn't a false disclosure), an explicit failure count, or a contrastive marker (`pre-existing`/`unrelated`/`known…`) that **co-occurs with a failure word in the same sentence** — all on the quote-stripped view. A clean `22/22` or "unrelated to the parser" still fires (Fable blocker: the guard must not become an off-switch). *Documented ceiling:* the disclosed magnitude isn't cross-checked against the output's fail count.

- **Class-9 fired on a COMMENT that merely NAMED the evaluator.** *Symptom:* the project's own AG-A comment `// … without GROUNDTRUTH_KEY …` self-matched as "source branches on the evaluator/CI." *Fix:* split the pattern — a branch-on-evaluator is tested on the **comment-stripped code portion** (a comment naming an env var is not a branch); a `groundtruth-disable` suppression *directive* is matched on the language-aware comment portion **anchored to its start** (so mid-prose `#groundtruth-ok` doesn't count).

*Method, unchanged:* for each fix, *"what's the cheapest way for a smart agent to make this go green without doing the work — and, now, what's the cheapest way this check fires on honest work?"* Remove the path, leave a test.

## v1.0.1 — the untracked secret scan reads the whole file, not the first 1 MB

- **A hardcoded secret past the 1 MB mark was not caught, only surfaced.** *Symptom:* the untracked-file secret scan read the whole file but scanned only its first 1 MB (`UNTRACKED_SCAN_CAP`), so a key on the last line of a larger file (e.g. a 3 MB data/JSON blob) slipped the deterministic secret check — it produced an amber *"scanned first 1 MB only, review the remainder"* nudge, not a secret finding. Nothing stops a dev (or an agent) from putting a key on the last line. *Fix:* scan each untracked file **in full up to 64 MB** — which covers every realistic secret-bearing file (`.env`/config/source/data) end to end, last line included — via a **bounded partial read** (`open`+`read` up to the cap) that replaces `readFileSync`-the-whole-file, so the read itself can no longer OOM the per-turn hook on a multi-GB blob. *Honest ceiling:* two hard constraints force *some* bound — the Stop hook runs every turn and must never hang, and Node caps a single string at ~512 MB, so a truly-unbounded whole-string scan is impossible. 64 MB is a tunable constant; anything larger is still **surfaced (`oversized`), never silently trusted**. Verified end-to-end: an AWS key on the last line of a 3 MB untracked file is now flagged as a hardcoded secret. *(v1.0.1 fixed the per-file read but not the aggregate — see v1.0.2.)*

## v1.0.2 — a TOTAL scan budget (v1.0.1 moved the OOM from the read to the consumer)

- **Raising the per-file cap 64× with no aggregate budget moved the failure from producer to consumer.** *Symptom (Fable, on review of v1.0.1):* the per-file read is bounded, but the scanned `content` accumulates across **all** untracked files and is regex-scanned as one string. A repo with a few large un-gitignored data files (no attacker needed) pushes `content` to hundreds of MB → the per-turn scan stalls (benched ~1.65 s / 1.2 GB for 4×64 MB); at ~8×64 MB `content +=` throws `RangeError: Invalid string length`, which the function's outer `catch` swallows — so every untracked file **after** the overflow is neither scanned nor surfaced (a padding bypass, violating the H2 "padding can't buy a green" principle one level up). *Fix:* a `UNTRACKED_TOTAL_CAP` (128 MB) across all untracked files — once the budget is spent, remaining files are **surfaced (`oversized`), never read or silently dropped**, so total `content` stays bounded (≈ budget + one per-file overshoot, safely < 512 MB) and the anti-gaming property holds. Both caps are now function parameters so the self-check drives tiny ones deterministically (scanned / per-file-truncated / budget-exhausted paths all covered). Also corrected: the oversized card message printed `67.108864 MB` (a `/1e6` divisor on a binary cap) and a comment overclaimed "bounds both read and scan memory" (true only per file). Verified at scale: 4×60 MB untracked files complete in ~160 ms, `content` bounded at 188 MB, the over-budget file surfaced.

## v1.0.3 — two untracked-scan coverage holes closed (Fable residuals from the v1.0.2 review)

- **An unreadable untracked file was silently skipped.** *Symptom:* a `chmod 000` file (read bit stripped) made `openSync` throw, and the handler just `continue`d — so a secret behind a removed read permission was neither scanned nor surfaced. *Fix:* on an open failure, push the file to `oversized` (`"unreadable (permission denied), review manually"`) — `statSync` needs only directory search, so the size still shows. Nothing hides behind a stripped read bit. (Guarded regression test: skips on root/CI where `chmod 000` stays readable.)
- **A repo with >1 MiB of untracked FILENAMES silently skipped the whole untracked scan.** *Symptom:* `execSync` defaults to a 1 MiB stdout buffer; a large-enough untracked file list overflowed it, threw into the outer `catch`, and dropped the entire untracked secret scan with no signal. *Fix:* set `maxBuffer` to 256 MiB on the `git status --porcelain` call — covers any real tree; the filename list is orders of magnitude smaller than that. Both were pre-existing (predate the v1.0.1/1.0.2 work), surfaced by Fable's v1.0.2 review and deferred, now closed.

## v1.0.4 — the oversized-file warn fired every turn on a throwaway `tmp/` blob

- **An `oversized` warn on a `tmp/` file re-fired on every single Stop — pure repetitive noise.** *Symptom (from a real hindsight session):* a 3 MB untracked `tmp/reconcile/diff.json` — never touched, never committed, explicitly kept out of every commit — produced *"untracked file too large to fully scan for secrets"* on **every turn** for the whole session. *Root cause:* `untrackedAdded()` skipped only the changed-file set and `.claude/groundtruth/`, **not** the throwaway paths (`tmp`/`temp`/`scratch`/`scratchpad`/absolute/`../`) that `excludedScanPath()` already knows aren't deliverables. Its `content` output was stripped of those paths downstream by `dropExcludedFiles()` — but the `oversized` list **bypassed that filter** and leaked straight into findings. So a throwaway blob that was already excluded from the actual secret scan still nagged, forever. *Fix:* skip `excludedScanPath(f)` at the **source** of the untracked loop (it's a strict superset of the old `.claude/groundtruth/` check), so a throwaway file is neither read into `content` nor surfaced as `oversized`. As a bonus, excluded files now consume **zero** scan budget, so more real content is actually scanned before the `UNTRACKED_TOTAL_CAP` is hit. *Not a security change:* an excluded path's content was **already never scanned** for secrets via the Stop path (`dropExcludedFiles` stripped it before `analyze` saw it), so removing the vestigial `oversized` warn takes away no coverage — the same secret in a *small* `tmp/` file was already invisible; the warn fired only when the throwaway happened to be huge.
- **Enforcement-boundary invariant pinned (Fable, on review).** The Stop path suppresses `tmp/` noise upstream, but the actual merge gate must not: pre-commit (`--pre-commit`) and CI (`--diff-range`) pass the **raw** diff to `analyze()`, which has no path filter — so a *committed* `tmp/` secret still blocks. Fable flagged that this now rests on a silent invariant ("don't move the exclusion into `analyze`"), so a regression test was added asserting `analyze()` still flags a secret on a `tmp/` path — it fails loudly if a future refactor unifies the exclusion and takes committed throwaway-path secrets dark at the boundary. Self-check **495 → 502**; red-team **14/14** (unchanged).

## v1.0.6 — Class-1 read "pass" the noun as "passes" the verb, and the fix that closed it opened a laundering hole

- **"then build the FIX pass" fired Class-1 — a plan announcement read as a green verdict.** *Symptom (10-day hindsight replay, 2026-07-06):* a turn describing a validation **stage** — "then build the FIX pass (verify pin → …)" — was flagged as a false "build passes" claim (at block severity, pre-demotion). *Root cause:* the keyword→verb gap (`[^.!?\n]*`) spans a whole noun phrase, so `build … pass` matched with `pass` as a NOUN. *Fix:* a noun guard — a bare `pass` headed by a determiner (+ optional modifier: "a pass", "the FIX pass", "a second pass") is a stage, not a verdict; only the bare form is ambiguous, inflected verbs (passes/passed) and green/verified/succeeded are untouched, and a real subject before it ("all tests pass") still fires.
- **The first-draft guard inspected only the match's FINAL verb — and the greedy gap makes that the LAST verb in the sentence, so a trailing noun-`pass` silenced a REAL leading claim (Fable, on review; verified FN).** "Tests pass after the second pass." / "The tests pass; next up: the FIX pass." / "The build is green after one pass over the diff." all went **silent** — an agent could launder any green claim by appending "— one more pass later". *Fix:* the guard walks **every** verb occurrence in the matched span; the match is excused only if none is a credible verb ("the FIX pass then the CONFIRM pass" stays excused).
- **The unbounded keyword→verb gap glued unrelated clauses into one claim (Fable, replay residual).** "…no named file/**test**, so the auditor had no explicit deliverable list to tick off (… the fix was **verified** …)" married a noun 18 words away from a verb across three clauses and warned. *Fix:* past `MAX_GAP` words the check **abstains** — warn-tier heuristic, abstention outside the provable scope, per the charter. *The bound is tuned against BOTH directions, not guessed:* an 8-word bound (the first draft) sat **below** a real claim — "the tests I added for the new billing and invoicing modules now all pass" spans 11 — and so silently **ate** it. Abstain **is** a clean green, which makes a too-tight gap an FN, not a safe default. **12** is the verified band: above the widest real claim (11), below the glue FP (18); ≥18 lets the FP back in. Both edges are pinned by regression tests.
- **Documented residuals (accepted, warn-tier):** (1) a DET inside a PP modifying a real subject — "tests on this branch pass" — is surface-identical to a noun stage ("lint in a final pass") and abstains; needs a parser to do better. (2) Third-person past-run narration — "Fable … ran `node --check` + the tests (green in raw node)" — still warns: the text genuinely asserts tests-green; its falseness (wrong session, someone else's run) is outside what a deterministic checker can observe, and adding `ran` to the reported-speech attribution verbs would excuse "I ran the tests and they pass" — the check's core case. Self-check **509 → 520**; red-team **14/14**; corpus benchmark unchanged.

## v1.0.5 — a pasted tool listing minted a phantom HARD deliverable (the block-mode wedge)

- **An UNFENCED pasted tool listing minted a phantom HARD deliverable — and phantom deliverables ESCALATE TO BLOCK.** *Symptom (real hindsight session, task `[tjqyi]`):* the operator pasted an MCP-server status listing raw — `Project MCPs (…/.mcp.json) ❯ supabase · ✔ connected …` — purely as context. The ledger read `.mcp.json` / `claude.json` as **requested deliverables**, minted a pending task, and since no commit ever touches those files it could **never close** — so every subsequent "done" tripped `false completion (async)` forever. *Root cause:* `pasteStripped()` recognized only **fenced** ```` ``` ```` blocks, `>`-quoted lines, and `file.js:42` stack refs. A raw pasted status/tool-output block — *the way people actually paste* — was read as prose, so its filenames became HARD deliverables. *Why it mattered now:* an open loop escalates to **`block`**, so with block mode enabled every pasted tool-output block naming a file would **wedge the agent on nothing** — the exact failure that makes an operator rip block back out. *Fix:* `pasteStripped()` also strips lines carrying a structural tool-output glyph (`❯ ⎿ ⏺ │ ├ └ ┌ ┐ ═ ║ …`), making their tokens paste-refs → **SOFT** (surfaced once, auto-expires, never blocks, still grounds to `done`) — per the demote-never-drop invariant; nothing vanishes. Whole-line strip is required, not glyph-onward: the motivating listing has `❯` **mid-line**, so anchoring would leave `.mcp.json` HARD and un-fix it. *Fable, on review:* `▶`/`►` were **removed** from the glyph class — they're the only ones a human writes in prose (`refactor parser.js ▶ tokenizer.js`), and they wrongly demoted a genuine ask. *Accepted trade (pinned by test):* an imperative sharing a line with a real tool glyph (vitest prints `❯`) demotes to soft; restating it on a glyph-free line restores HARD. *Known-narrow (unchanged from before):* glyph-less pastes (plain `ls`, ASCII `|` tables) still mint. *Tests:* the MCP listing, Claude Code's own `⏺`/`⎿` output, the prose-arrow ask (stays HARD), the tool-glyph imperative (soft), and its recovery path. Self-check **502 → 509**; red-team **14/14**.

## v1.1.0 — Class-1 evidence goes artifact-grounded: exit status, ordering, filtered runs (the three confirmed holes)

An external review found — and live probes against v1.0.6 confirmed — that Class-1 decided "did the tests actually pass" from **command presence + a stdout substring**, discarding the two artifact signals the transcript already carried. The architectural root cause: `parseTranscript` collapsed the transcript into two **unpaired flat arrays** (`bashCmds`, `results`), so *which* command failed and *when* it ran relative to the edits were structurally unanswerable. v1.1.0 adds a paired + ordered evidence layer (**additive** — the flat arrays and every legacy caller are untouched) and three checks on top of it.

- **Exit code was captured and read by nothing.** *Symptom (confirmed silent):* `npm test` → `is_error:true` with output `"Error: command failed with exit code 1"` or `"Killed"` (OOM) — no string `TEST_FAIL_RE` recognizes — blessed a "tests pass" claim. *Root cause:* failure was inferred **only** from the stdout substring sensor; the recorded exit status never reached Class-1. *Fix:* `parseTranscript` now pairs each Bash `tool_use.id` with its `tool_result.tool_use_id` (`bashEvents`); a **non-zero exit on the last completed test/build run** contradicts a pass claim regardless of stdout. Scope guards, each a real FP class: last **completed** run only (red → fix → re-run-green stays silent); harness aborts (user interrupt / permission rejection / timeout) are no-result, not failure; the exit must be **attributable** — in `npm test && git commit` the pipeline exit is the last segment's, so a failed commit after green tests abstains; background runs excluded; an honest disclosure ("15/16 pass") suppresses, same as the substring branch.
- **A green run that PREDATES the final source edit read as current.** *Symptom (confirmed silent):* Bash `npm test` → "5 passed" → **Write src/billing.js** → "Done. All tests pass." — the green genuinely predates the last mutation; nothing noticed. *Root cause:* no signal recorded the relative order of Bash vs Edit/Write/MultiEdit calls. *Fix:* a monotonic `seq` over **all** tool_use blocks (transcript order — the harness's own append-ordered record, one of the two unforgeable anchors) plus an ordered `mutations` ledger; a last-mutation seq past the last test-run seq → **stale green** warn. Mutation scope is deliberately narrow (charter: provable scope or abstain): CODE files only (a README/CLAUDE.md edit after green stays silent; accepted FN: package.json/tsconfig ride out with the non-code extensions), no throwaway/tool paths, and the edit's **changed lines must contain code** — the ledger set-diffs away the unchanged context lines an Edit carries for `old_string` uniqueness, so a comment-only tweak can't go stale a green, while a pure deletion of code (old side kept in the set-diff) still counts. Any background test run → the ordering check abstains entirely (its completion order is unknowable).
- **A filtered run backed an "all tests" claim.** *Symptom (confirmed silent):* `npm test -- --grep foo` / `pytest -k billing` matched `TEST_BUILD_RE` on the prefix → `ran=true` → "all tests pass" blessed. *Fix (provably scoped, else abstain):* fires only when (a) the claim's own sentence asserts universality with the quantifier **adjacent** ("all tests" / "entire|whole|full [test] suite" / "every test") — "all 12 billing tests pass" after `pytest -k billing` is honest and the adjacency gate keeps it silent — and (b) **every** test segment run this session carries an unambiguous narrowing flag. Short flags are runner-scoped where they collide: `-k` counts only under pytest/tox (`make -k` is keep-going), `-t` only under jest/vitest (`mocha -t` is a timeout), `-m` dropped entirely (`python -m pytest` is a full run). *Documented ceiling, not a guess:* a bare file arg (`pytest tests/test_billing.py`) is **not** detected — a one-file repo's whole suite IS one file (this repo's own `node hooks/groundtruth.test.mjs`).
- **Severity: all three are WARN, not block — deliberately.** The evidence side is artifact-grounded (paired exits, transcript order), but every one fires only under `_passClaim` — a prose-parsed natural-language trigger with a live-demonstrated FP class — and a conjunction's severity is bounded by its weakest conjunct (the same rule that demoted the `!ran` branch after its silent block-loop). The chain stays `else-if`, most-grounded first, so a failed+stale run yields ONE finding, not a stack.
- **Abstention is structural, not incidental.** `bashEvents`/`mutations` are optional `analyze()` inputs passed **only by the Stop path**; `--pre-commit` and `--diff-range` never construct them, and a transcript without tool ids (predating this change, or a foreign harness) yields unpaired events (`is_error:null`) — in every such case the new checks emit nothing and bless nothing, and the flat-array sensors keep their previous coverage unchanged. Pinned by abstention regression tests per check.
- **Accepted residuals (each pinned or documented):** a Bash-channel mutation (`sed -i` after the green run) is invisible to the mutation ledger → stale-green FN; an edit inside an open block comment can read as code via fresh per-edit lexer state → a factually-true-but-noisy stale warn (warn-tier, message says "re-run to confirm"); `npm test -- -k foo` (filter passed through npm to an unknown runner) is not runner-attributable → filtered-run FN. Self-check **521 → 553**; red-team **14/14**; corpus benchmark unchanged (17/23 at-capture).

### v1.1.0 adversarial-review pass (Fable) — one inert-in-production check, two verified FPs, two laundering FNs

- **Stale-green shipped silently INERT — dead on every real transcript while its own tests stayed green (the cardinal sin).** *Symptom (verified against live transcripts on this machine):* `parseTranscript` → `analyze` with a real green-then-edit sequence produced **no** stale finding. *Root cause:* real transcripts record **absolute** `file_path` (the Edit/Write tool schema requires it), and `excludedScanPath` — built for git-relative paths — reads any absolute path as out-of-tree, so `codeMuts` was **always empty** on the one path that supplies mutations (Stop). The unit tests used relative fixture paths and passed. *Fix:* relativize each mutation path against `cwd` (slash-normalized, case-insensitive prefix) before the `CODE_EXT_RE`/`excludedScanPath` gates; a path that stays absolute is genuinely out-of-repo (a scratchpad write) and stays excluded. *Regression:* absolute in-repo path fires; absolute out-of-repo path stays silent.
- **A TRUE "the tests pass" was warned NON-ZERO because `npm run lint` failed afterwards (verified FP) — and the mirror-image laundering hole (verified FN).** *Root cause:* `TEST_BUILD_RE` spans tests+build+lint+typecheck as one family; the exit/stale/filtered checks inherited that breadth, so a later lint failure "contradicted" a tests claim, a green lint run after the last edit **refreshed** a stale test green, and an unfiltered `npm run lint` beside `pytest -k billing` defeated the filtered-run `.every`. *Fix:* a **family fence** on the three artifact-grounded checks only (the legacy `ran`/`onlyWeak`/`failed` sensors keep their breadth — zero legacy drift, re-proven by running the pre-change 509-check suite against the fixed engine): lint segments count only under a lint claim and never under any other noun; build/typecheck stay relevant to a tests claim on purpose (code that doesn't compile can't honestly be "tests pass"). `exitAttributable` also requires the final segment to be in-family (`npm test && npm run lint` failing under a tests claim abstains — the pipeline exit is the lint's). The filtered check grades family hits with a non-empty guard (`every([])` was a phantom "all runs filtered" verdict when only lint ran).
- **A multi-line Bash call attributed a failed line-2 commit to the line-1 test (verified FP).** *Root cause:* the shell-segment split (`&&`, `||`, `|`, `;`) omitted `\n`, so `npm test\ngit commit -m x` was ONE segment that matched `TEST_BUILD_RE` → "attributable". *Fix:* shared `SHELL_SEG_RE` adds `\n`, used by `exitAttributable`, `filteredOnly`, **and** `weakOnly` (whose own latent multi-line FP — `tsc\nnpm test` read as a bare typecheck — the same split fixes; the direction is strictly fewer warns, so no legacy FN risk).
- **Mixed pairing condemned on missing data.** *Symptom (constructed):* a completed RED run followed by an UNPAIRED later run (`is_error:null`) fired the exit warn off the earlier red — but the lost result may have been the green re-run. A live Stop transcript pairs every foreground Bash before the final claim exists, so mixed pairing means a truncated/foreign transcript. *Fix:* any unpaired foreground test event → the exit check abstains entirely; `is_error:null` is abstain in **both** directions, never "didn't happen".
- **New accepted residuals (pinned):** a verdict phrased DET+modifier+`pass` ("The tests: **a clean pass**") is surface-identical to a stage name ("a full pass over the docs") and abstains — fixing it resurrects the v1.0.6 noun-stage FP class; an exit-1 run whose own stdout echoes the harness-abort marker ("Command timed out after …") reads as an abort — stdout is agent-authorable text, warn-tier FN. Self-check **553 → 566**; legacy pre-change suite **509/509** against the fixed engine; red-team **14/14**; corpus unchanged (17/23 at-capture).

## v1.1.1 — the orchestrator false positive, exit-attribution across shell connectors, and inert-edit noise

Three holes surfaced by putting the v1.1.0 evidence layer through two Fable passes (a break-it red-team and an architectural review) plus a real subagent transcript on this machine. All three are warn-tier and transcript-anchored — no new hook, no on-disk snapshot (a file-checksum design was weighed and **rejected**: the snapshot is agent-writable disk state, which SECURITY.md holds is never a boundary — a forgeable seal closes nothing against the laundering case and isn't needed for the benign one).

- **Every test-running SUBAGENT was falsely accused "no test/build command ran" (structural FP, verified live).** *Symptom:* at `SubagentStop` the payload's `transcript_path` is the subagent's OWN file (every entry `isSidechain:true`) and `last_assistant_message` is its "done" — but `parseTranscript`'s sidechain filter (correct for the main path, where a subagent's turns are the orchestrator's prompts, not human asks) deleted **100% of the subagent's evidence while keeping its claim**. An honest subagent that ran `npm test` green and reported "all 502 tests pass" hit the `!ran` branch. *Worse:* the same stop ran the shared-session writes against the ORCHESTRATOR's contract — overwrote `<session>.md`, advanced the referee snapshot mark (eating the orchestrator's ratification window), rebuilt `tasks.json` from the subagent's non-asks, and in block mode burned the shared 2-attempt cap with no per-agent state behind it. *Fix:* `main()` now branches on `payload.hook_event_name === 'SubagentStop'`: parse the subagent's own file with `parseTranscript(text, { includeSidechain: true })`, grade its claim against its own tool-ledger evidence, **write nothing shared** (no `<session>.md`, no `findings.json`, no snapshot advance, no `tasks.json`, no attempts), and **demote any block-tier finding to warn** structurally regardless of `GROUNDTRUTH_BLOCK` — a real block-tier catch (a secret in the ledger) regains its tier at the orchestrator's own Stop, which re-scans the same tree. An unreadable/absent subagent transcript → abstain (exit 0), never re-mint the FP. *Regression:* an all-sidechain subagent transcript + a green run + "all tests pass" → silent; the same with a genuinely failed run → still fires; the main Stop path still filters sidechain; SubagentStop never returns block even under `GROUNDTRUTH_BLOCK=1`.
- **Exit attribution was wrong across every shell connector (one FN, one FP, one laundering FN).** *Symptoms:* `npm test && echo ok` that crashes (`is_error:true`) stayed **silent** — a five-char trailing segment laundered the failure; `cd frontend && npm test` where `cd` fails **fired** — the test never ran, but the shipped "last segment in-family" rule attributed `cd`'s exit to it; and after a red run, re-running `npm test || true` (or `| tee log`, or `; echo ok`) recorded a green `is_error:false`, became the run-of-record, and permanently **laundered the red away**. *Root cause:* the attribution rule reasoned about the last *segment* without connector semantics — `&&` short-circuits, `||` masks, `|` takes the last stage's exit, `;` takes the last statement's. *Fix (connector-aware, ~15 lines):* (1) a command containing `||` anywhere → the run is exit-NULL (masking); (2) split into statements on `;`/`\n`, the exit belongs to the last statement only — an in-family segment in an earlier statement → exit-NULL for the family; (3) within the last statement's `&&`-chain/pipe, attributable iff ≥1 segment is in-family AND **every other segment is infallible** (allowlist: exactly `echo`, `true`, `:` — `cd` is NOT infallible, so `cd x && npm test` abstains); (4) a NULL latest family run abstains entirely and can never displace an earlier attributable red — mirroring the existing unpaired-foreground abstain-in-both-directions. Now `npm test && echo ok` fires, `rm x && npm test` / `cd x && npm test` abstain, and masked/piped re-runs neither fire nor bless (the legacy stdout sensor still independently catches a `|| true` run whose output prints "3 failed"). *Regression:* one assertion per connector case above.
- **Stale-green fired on inert edits (verified FP).** *Symptom:* a comment tweak on a line that also carries code (`total++; // count` → `total++; // the count`) and a pure whitespace reformat both tripped stale-green after a green run — provably behavior-neutral, so a false accusation on the single most common honest way to end a turn. (A standalone comment-only line already stayed silent; a string-literal change correctly **fires** — a changed SQL/regex/message string is behavioral, and suppressing it would be an FN.) *Root cause:* the mutation-is-code test was line-granular — "the changed line contains any code" — so a differing comment or reindent on a code line counted. *Fix:* the ledger set-diff now compares the **normalized code portion** of each changed line — `splitCodeComment(line).code` with internal whitespace collapsed — so a comment/whitespace-only delta normalizes equal and doesn't count, while a changed string still differs and fires. No new hook or on-disk state; the same transcript-anchored ledger. *Documented residual (unchanged from v1.1.0):* a Bash-channel edit (`sed -i` after the green run) is invisible to the ledger — the honest next rung is a transcript-grounded command sensor (a `sed -i`/`tee` targeting a code path after the last test seq), same anchor class as the referee-tamper check, not a disk snapshot. Self-check **566 → 588**; red-team **14/14**; corpus unchanged (17/23 at-capture).

### v1.1.1 adversarial-review pass (Fable) — two laundering FNs, three verified FPs, all in the new code

- **A one-char suffix laundered a red: `npm test;` (or a trailing newline) crashed SILENT.** *Root cause:* the statement split kept BLANK statements, so `npm test;` → `['npm test','']` put the real run in an "earlier" statement whose exit rule (2) discards — the exact laundering class rule (3) exists to close, reopened by an empty string. *Fix:* blank statements are dropped before the rules run (`.filter(s => s.trim())`, plus an empty-list guard).
- **A lone `&` was no separator: `npm test & git push` failing attributed the push's exit-1 to the test (verified FP).** `a & b` backgrounds `a`, so the compound's exit is `b`'s — exactly `;` semantics. *Fix:* the statement split adds lone `&` with lookarounds that keep `&&` whole and never split `>&`/`&>` (redirect fd syntax — naively splitting there turned the ubiquitous `npm test 2>&1` into two statements and NULLed a real red; pinned by test).
- **`echo` is NOT infallible with a redirection: `npm test && echo done > /bad/path` fired on green tests (verified FP).** The exit-1 is the echo's failed redirect. *Fix:* the allowlist entry is end-anchored and rejects any `<`/`>` in the segment; `echo $(x)` stays infallible (a substitution's exit is discarded — `npm test && echo $(cleanup)` crashing still fires, correctly).
- **A runner name in an ARGUMENT made a non-run "family": `grep vitest package.json` exiting 1 (no match) after a genuine green read as a FAILED test (verified FP) — and the same grep run after the last edit refreshed `lastTestSeq` and washed a stale green dark (verified laundering FN).** *Root cause:* the artifact checks inherited TEST_BUILD_RE's deliberate \b-substring breadth, which is safe only in the legacy sensors' bless direction. *Fix:* `FAM_CMD_RE` — family for the artifact checks requires the runner in **command position** (optional subshell paren / env-var prefixes / npx-style wrappers / path prefix, the same prefix idiom `WEAK_CHECK_RE` uses). The legacy `ran`/`onlyWeak`/`failed` sensors keep the substring breadth untouched — zero legacy drift, re-proven by running the committed pre-v1.1.x suite (509/509) against the fixed engine. *Kept breadth, on purpose:* `make clean` failing still counts against a tests claim — `make <any-target>` is build-family by TEST_BUILD_RE design (Makefile semantics are unknowable), matching the legacy sensors.
- **An EMPTY-but-readable subagent transcript re-minted the exact `!ran` FP the SubagentStop branch removes (verified via the CLI).** `readFileSync` succeeds on a 0-byte file, so the claim was graded against zero evidence. *Fix:* zero evidence AND zero task text (a real subagent transcript always carries at least its task prompt as intent) → abstain, same as unreadable. A lazy-but-real subagent (prompt present, no run) is still graded and still caught.
- **Accepted residuals (all abstain-direction, warn-tier):** a quoted connector (`npm test && echo "a || b"`) reads as masking → a real red abstains (a shell-grammar parser is the only fix; FN, never FP); the mutation set-diff is order/count-blind (a pure line REORDER or dedup normalizes away — pre-existing v1.1.0 ledger semantics, not a normalization regression); an Edit fragment cut mid-string (`s = "a // b` unterminated) reads its tail as comment (pre-existing `blankStrings` line scope). Self-check **588 → 596**; legacy committed suite **509/509**; red-team **14/14**; corpus unchanged (17/23 at-capture).
